package lesson10;

enum Secret {
    STAR, CRASH, START;

    public static void main(String[] args) {
        int counter=0;
        for(Secret val:values())
            if(val.name().startsWith("STAR"))
                counter++;
        System.out.println(counter);

    }
}
